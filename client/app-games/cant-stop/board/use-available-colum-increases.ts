// Returns an array of arrays, each inner array reducing the indices of the
// columns on the Cant Stop board that the play can choice to increase.
export function useAvailableColumnIncreases() : number[][] {
    // dummy values for now
    return [
        [2, 3],
        [3, 5],
        [12],
    ]
}